[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Override this in Netlify's dashboard (Site settings -> Environment variables)
  # with your deployed backend's URL. This is just a local-dev fallback.
  VITE_API_BASE = "http://localhost:4000"
